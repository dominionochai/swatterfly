# Dashboard integration placeholder

The Stitch cockpit design is preserved as a product requirement in
`docs/06-cockpit.md`. This directory is intentionally not a second UI design.

TODO: connect the future TypeScript/Next.js dashboard to a typed telemetry
stream, replay files, target-lock state, and read-only safety indicators. Keep
flight control authority outside the browser.
