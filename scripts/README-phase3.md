# Phase 3 baseline analysis
Run from the repository root; the script resolves `repo_root` from its own path.
Install: `python -m pip install numpy pandas matplotlib`
Run: `python scripts/phase3_baseline_analysis.py`; outputs are new analysis CSVs, a Markdown report, and an Agg PNG under `data/phase3_baseline/` (existing `results.csv` is preserved).
